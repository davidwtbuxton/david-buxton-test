application: david-buxton-test
version: dev
runtime: python27
api_version: 1
threadsafe: true

builtins:
  - deferred: on

handlers:
  - url: /tasks/.*
    script: wsgi.app
    login: admin

  - url: /site.css
    static_files: site.css
    upload: site.css

  - url: /.*
    script: wsgi.app

skip_files:
  - ^(.*/)?#.*#$
  - ^(.*/)?.*~$
  - ^(.*/)?.*\.py[co]$
  - ^(.*/)?.*/RCS/.*$
  - ^(.*/)?\..*$

  - ^libs/.*\.dist-info/
